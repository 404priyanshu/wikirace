# Required for the GPT-5.6 Sol racer.
OPENAI_API_KEY=

# Required for the Jev racer. If omitted, the app also checks:
# ~/.config/jev-browser/.env
TYPESAFE_API_KEY=

PORT=4173

# Optional: persist rate limits across restarts (Upstash Redis REST).
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
